# 项目协作规则（CLAUDE.md）

> 本文件在每次 session 启动时都会被加载，请严格遵守以下规则。
> 如果规则之间冲突，以"自我保护规则"优先级最高。

## 0. 自我保护规则（最高优先级）

- **禁止修改本文件（CLAUDE.md）**，除非用户在当前对话中明确说"帮我改 CLAUDE.md"或"更新一下项目规则"。
- 如果发现本文件内容与实际情况不符（比如规则过时），**不要自行修改**，而是提醒用户："CLAUDE.md 里的 XX 规则似乎需要更新，需要我改吗？"
- 不要在自动化任务（如批量重构、格式化）中把本文件当作普通文件一并修改。

## 1. 状态持久化规则（防止 context 压缩丢细节）

- 项目根目录维护一个 `.claude/STATE.md`，用于记录当前任务的完整状态。
- 每完成一个有意义的子任务（如修复一个 bug、完成一个功能点），**立即**在 `.claude/STATE.md` 追加记录，格式：

  ```
  ## [日期] 任务名
  - 状态：已完成 / 进行中 / 待处理
  - 优先级：P0-P5
  - 描述：xxx
  - 涉及文件：path/to/file.js
  - 备注：xxx
  ```

- 每次开始新任务前，**先读取 `.claude/STATE.md`**，以此为准，不要仅依赖对话历史中的记忆。
- 如果一次性列出了多个待办事项（比如 bug 清单、任务清单），**必须先写入 `.claude/STATE.md` 或专门的清单文件（如 `.claude/BUGS.md`）**，再开始逐条处理。
- 处理清单时，每处理完一项就更新对应文件里的状态，不要等全部做完再一次性更新。

## 2. 上下文管理

- 如果感知到当前对话已经很长（用户提到"上下文快满了""context 80%"等），主动提醒用户：先把当前状态写入文件，再考虑开新 session 或执行 `/compact`。
- 不要假设自己"记得"很久之前对话中的细节，涉及关键决策、清单类信息时，优先以文件内容为准。

## 3. 代码修改规范

- 修改代码前，先说明将要做的改动范围，避免大范围无关改动。
- 每次修改后，如果项目有测试，运行相关测试确认没有引入新问题。
- 不确定的地方，先提问或说明假设，不要臆造需求。

## 4. Git 提交规范

- commit message 使用简洁的祈使句，格式：`<类型>: <说明>`，类型包括 feat / fix / refactor / docs / test / chore。
- 不要自动执行 `git push`，除非用户明确要求。

## 5. 沟通风格

- 回复中直接给出结论和改动内容，避免不必要的寒暄。
- 涉及不确定的技术判断时，明确说明"这是推测"还是"这是确认过的事实"。

---

## 待办 / 清单文件索引

- 当前 bug 清单：`.claude/BUGS.md`
- 项目状态快照：`.claude/STATE.md`
- （如项目有其他清单文件，请在这里补充索引）